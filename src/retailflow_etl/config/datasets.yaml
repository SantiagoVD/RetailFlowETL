datasets:
  sales: {input_format: csv, primary_key: sale_id}
  customers: {input_format: csv, primary_key: customer_id}
  products: {input_format: csv, primary_key: product_id}
  stores: {input_format: csv, primary_key: store_id}
  payments: {input_format: json, primary_key: payment_id}
  inventory: {input_format: xlsx, primary_key: inventory_id}
